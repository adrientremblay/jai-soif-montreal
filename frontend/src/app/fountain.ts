export interface Fountain {
    id: number;
    borough: string;
    placeName: string;
    placeType: string;
    intersection: string;
    notes: string;
    lattitude: number;
    longitude: number;
}